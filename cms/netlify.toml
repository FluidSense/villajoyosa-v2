[build]
  base = "cms/"
  publish = "dist"
  command = "npm run build"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF ."

 [[redirects]]
    from = "/*"
    to = "/index.html"
    status = 200
